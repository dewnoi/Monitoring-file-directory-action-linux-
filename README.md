# Monitoring-file-directory-action-linux-

1. Clone these repository

2. run "make" in terminal

3. run "sudo insmod monitor_kmod.ko"

4. run "sudo ./monitor <folder-path>"

5. After finished "sudo rmmod monitor_kmod"
